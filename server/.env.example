PORT=4000
# Comma-separated allowlist. Use * to allow all (not recommended).
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173
# Reverse proxy support (Render/Nginx/Cloudflare). Examples: 1, true, loopback
TRUST_PROXY=1
JWT_SECRET=change_me_please
# Upload storage: "disk" (default) or "db" (store in Postgres, good for free hosting).
UPLOAD_STORAGE=disk
# Optional: absolute path for persistent uploads when using disk storage (e.g. /var/data/uploads).
UPLOAD_DIR=
# Web Push (generate with: npx web-push generate-vapid-keys)
WEB_PUSH_SUBJECT=mailto:admin@example.com
WEB_PUSH_PUBLIC_KEY=
WEB_PUSH_PRIVATE_KEY=

# Database connection.
# If DATABASE_URL is set, it takes precedence and PG* variables are ignored.
DATABASE_URL=postgres://elia:elia_pass@localhost:5432/elia_messenger

PGHOST=localhost
PGPORT=5432
PGUSER=elia
PGPASSWORD=elia_pass
PGDATABASE=elia_messenger
